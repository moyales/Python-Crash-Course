#Exercise 4.5: Summing a Million
#Make a list of the numbers from one to a million and use the min/max functions to check the end values.
numbers = list(range(1, 1000001))
print(min(numbers))
print(max(numbers))
print(sum(numbers))
